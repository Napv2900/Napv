print()- выводит значения
int('Строка',СС)
if(условие):
 тело
 for i in range(от скольки, до скольки, шаг):
  тело цикла 
 i-счётчик 
 while(условие):
  тело цикла 
 input()-ввод значений 
 with open('имя файла') as f: - открытие текстового файла
 a=f.readline() чтение файла 
 a=f.replace('C','S') меняем одно значение на другое 
 if all(num%delit!=0 for delit in range(2,num)):
  тело
 break принудительно закончить программу 
def f(параметр):
 тело функции
 count() - возвращает кол-во раз, сколько данный элемент появлялся в списке 
 str() меняет на строку 
 sorted() - сортирует элементы списка 
 reversed() - инвертирует порядок элеметов списка
 .index() - пишет индекс элемента в списке 
 .append() - добавить элемент к списку
 .split(' ') - разделить строку на элементы списка по разделителю 
left(90) - поворот черепашки влево на 90 градусов
right(90) - поворот черепашки вправо на 90 градусов
forward(300) - движение черепашки на 300 вперёд по направлению, куда она смотрит
goto(x,y) - перепещение на координаты (x,y)
dot(5) - точка диаметром 5
done() - завершение работы черепашки
pu() - перемещение черепашки больше не оставляет следов
abs() - модуль числа
max() - нахождение максимального значения
min() - нахождение минимального значения
list() - преобразование к списку
